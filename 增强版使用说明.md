# Token Meter 增强版使用说明

## 1. 概述与核心特性

本项目基于 Splunk 官方开源的 [token-meter](https://github.com/splunk/token-meter) 构建，通过**无侵入补丁与独立运行时适配层**，在不修改任何官方源码的前提下，实现了以下三项增强能力：

1. **界面全中文汉化**：
   - 包含导航栏、看板 KPI 卡片、使用明细、模型趋势、花费统计、效率洞察、Git 交付、工具能力和系统设置等全模块深度汉化。
   - 结合静态预渲染与动态本地化 Runtime 引擎（`MutationObserver`），确保异步刷新的状态和动态卡片实时呈现中文。
2. **新增三款主流 AI 编码工具支持**：
   - **Antigravity (Google / Codeium)**：自动读取 `~/.gemini/antigravity/conversations/*.db`，精准解析 SQLite 元数据与 Protobuf Token 消耗（支持 Gemini 3.8/3.6/3.1/2.5 系列）。
   - **WorkBuddy (腾讯混元)**：自动读取 `~/.workbuddy/workbuddy.db` 与 `~/.workbuddy-ai/workbuddy.db` 的会话及用量表，按混元费率核算。
   - **Trae CN (字节跳动国内版)**：自动读取 `%APPDATA%\Trae CN\User\globalStorage\state.vscdb`，解析会话、草稿与所选模型（Doubao-Seed-Code、GLM-5.3-flash 等）。
3. **防覆盖升级机制 (Update-Safe)**：
   - 所有汉化与适配文件独立存放于 `custom/`、`page_zh.html`、`meter_custom.py` 中。
   - 通过 `.git/info/exclude` 本地物理隔离，`git status` 保持 100% Clean。
   - **后续执行 `git pull origin main` 升级官方代码时，绝不会发生冲突，也不会覆盖任何自定义功能**。

---

## 2. 快速启动与日常使用

| 操作 | 说明 |
| :--- | :--- |
| **一键启动** | 双击根目录下的 **`启动服务.bat`**（或 `run.bat`），窗口会自动在后台启动服务并呼出浏览器打开 `http://127.0.0.1:8722`。 |
| **一键停止** | 双击根目录下的 **`停止服务.bat`**，即可优雅释放后台进程并清理端口占用。 |
| **开机自启 (可选)** | 按 `Win + R` 输入 `shell:startup`，将 `启动服务.bat` 的快捷方式放入启动文件夹即可。 |

---

## 3. 架构与目录结构

```text
D:\tools\token-meter/
├── 启动服务.bat          # 增强版一键启动脚本（配置中文环境变量与自定义入口）
├── 停止服务.bat          # 一键停止端口 8722 上的服务进程
├── run.bat               # 快捷启动入口
├── meter_custom.py       # 扩展启动入口（加载补丁并启动官方服务）
├── page_zh.html          # 深度中文化独立前端文件（官方 page.html 原封未动）
├── custom/               # 独立扩展模块目录
│   ├── build_zh_page.py  # 中文页面重构脚本（官方更新 page.html 后可一键重构）
│   ├── models_pricing.py # Gemini、豆包、混元、GLM 各模型费率配置
│   ├── antigravity_adapter.py # Antigravity 本地数据提取适配器
│   ├── workbuddy_adapter.py   # WorkBuddy 本地数据库适配器
│   ├── traecn_adapter.py      # Trae CN 本地存储适配器
│   └── patcher.py        # 运行时动态 Hook 注入器
└── token_meter/          # 官方原始核心代码（保持 100% 纯净，随时支持 git pull）
```

---

## 4. 后续升级指引

当官方仓库发布新版本，需要拉取最新功能时，在仓库目录下打开终端执行：

```powershell
# 1. 直接拉取官方最新代码（绝不会发生冲突）
git pull origin main

# 2. (可选) 如果官方更新了前端布局，一键重新生成中文页面
python -X utf8 custom/build_zh_page.py

# 3. 重新双击 启动服务.bat 即可
```
